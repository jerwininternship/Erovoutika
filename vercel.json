{
  "version": 2,
  "buildCommand": "npm run build:client",
  "outputDirectory": "dist",
  "framework": null,
  "rootDirectory": "Dynamic-Brandings",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
